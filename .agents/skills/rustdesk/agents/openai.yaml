interface:
  display_name: "Rustdesk"
  short_description: "Repo-specific patterns and workflows for rustdesk"
  default_prompt: "Use the rustdesk repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true